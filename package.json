{
  "name": "fin-ass-we",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cpreview": "npm run build && wrangler pages dev .svelte-kit/cloudflare",
    "cdeploy": "npm run build && wrangler pages deploy .svelte-kit/cloudflare"
  },
  "devDependencies": {
    "svelte": "^4.2.12",
    "vite": "^5.0.0",
    "axios": "^0.28.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "wrangler": "^3.34.2"
  }
}
