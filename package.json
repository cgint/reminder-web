{
  "name": "reminder-web",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler pages dev dist --live-reload",
    "deploy": "npm run build && wrangler pages deploy dist",
    "watch": "npm-watch",
    "svelte-check": "svelte-check"
  },
  "devDependencies": {
    "svelte": "^4.2.12",
    "vite": "^5.0.0",
    "axios": "^0.28.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "rollup": "",
    "wrangler": "^3.34.2",
    "bootstrap": "^5.3.3",
    "npm-watch": "^0.11.0",
    "svelte-check": "^3.6.8"
  },
  "watch": {
    "build": {
      "patterns": [
        "src",
        "index.html"
      ],
      "extensions": "*"
    }
  }
}
